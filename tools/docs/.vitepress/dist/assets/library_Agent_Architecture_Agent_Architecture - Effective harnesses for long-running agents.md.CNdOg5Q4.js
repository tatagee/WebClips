import{_ as t,o as a,c as n,ag as s}from"./chunks/framework.eQLvDOR_.js";const u=JSON.parse('{"title":"Effective harnesses for long-running agents","description":"","frontmatter":{"title":"Effective harnesses for long-running agents","source":"https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents","category":"[Agent_Architecture]","clipped_at":"2026-04-04","tags":["agent-architecture"],"status":"distilled","compiled":true},"headers":[],"relativePath":"library/Agent_Architecture/Agent_Architecture - Effective harnesses for long-running agents.md","filePath":"library/Agent_Architecture/Agent_Architecture - Effective harnesses for long-running agents.md","lastUpdated":null}'),o={name:"library/Agent_Architecture/Agent_Architecture - Effective harnesses for long-running agents.md"};function i(r,e,l,d,p,h){return a(),n("div",null,[...e[0]||(e[0]=[s(`<p>As AI agents become more capable, developers are increasingly asking them to take on complex tasks requiring work that spans hours, or even days. However, getting agents to make consistent progress across multiple context windows remains an open problem.</p><p>The core challenge of long-running agents is that they must work in discrete sessions, and each new session begins with no memory of what came before. Imagine a software project staffed by engineers working in shifts, where each new engineer arrives with no memory of what happened on the previous shift. Because context windows are limited, and because most complex projects cannot be completed within a single window, agents need a way to bridge the gap between coding sessions.</p><p>We developed a two-fold solution to enable the <a href="https://platform.claude.com/docs/en/agent-sdk/overview" target="_blank" rel="noreferrer">Claude Agent SDK</a> to work effectively across many context windows: an <strong>initializer agent</strong> that sets up the environment on the first run, and a <strong>coding agent</strong> that is tasked with making incremental progress in every session, while leaving clear artifacts for the next session. You can find code examples in the accompanying <a href="https://github.com/anthropics/claude-quickstarts/tree/main/autonomous-coding" target="_blank" rel="noreferrer">quickstart.</a></p><h2 id="the-long-running-agent-problem" tabindex="-1">The long-running agent problem <a class="header-anchor" href="#the-long-running-agent-problem" aria-label="Permalink to &quot;The long-running agent problem&quot;">​</a></h2><p>The Claude Agent SDK is a powerful, general-purpose agent harness adept at coding, as well as other tasks that require the model to use tools to gather context, plan, and execute. It has context management capabilities such as compaction, which enables an agent to work on a task without exhausting the context window. Theoretically, given this setup, it should be possible for an agent to continue to do useful work for an arbitrarily long time.</p><p>However, compaction isn’t sufficient. Out of the box, even a frontier coding model like Opus 4.5 running on the Claude Agent SDK in a loop across multiple context windows will fall short of building a production-quality web app if it’s only given a high-level prompt, such as “build a clone of <a href="http://claude.ai/redirect/website.v1.52cdad0c-ca7a-43d0-8297-e3f64b8a4f36" target="_blank" rel="noreferrer">claude.ai</a>.”</p><p>Claude’s failures manifested in two patterns. First, the agent tended to try to do too much at once—essentially to attempt to one-shot the app. Often, this led to the model running out of context in the middle of its implementation, leaving the next session to start with a feature half-implemented and undocumented. The agent would then have to guess at what had happened, and spend substantial time trying to get the basic app working again. This happens even with compaction, which doesn’t always pass perfectly clear instructions to the next agent.</p><p>A second failure mode would often occur later in a project. After some features had already been built, a later agent instance would look around, see that progress had been made, and declare the job done.</p><p>This decomposes the problem into two parts. First, we need to set up an initial environment that lays the foundation for <em>all</em> the features that a given prompt requires, which sets up the agent to work step-by-step and feature-by-feature. Second, we should prompt each agent to make incremental progress towards its goal while also leaving the environment in a clean state at the end of a session. By “clean state” we mean the kind of code that would be appropriate for merging to a main branch: there are no major bugs, the code is orderly and well-documented, and in general, a developer could easily begin work on a new feature without first having to clean up an unrelated mess.</p><p>When experimenting internally, we addressed these problems using a two-part solution:</p><ol><li>Initializer agent: The very first agent session uses a specialized prompt that asks the model to set up the initial environment: an <code>init.sh</code> script, a claude-progress.txt file that keeps a log of what agents have done, and an initial git commit that shows what files were added.</li><li>Coding agent: Every subsequent session asks the model to make incremental progress, then leave structured updates.<sup>1</sup></li></ol><p>The key insight here was finding a way for agents to quickly understand the state of work when starting with a fresh context window, which is accomplished with the claude-progress.txt file alongside the git history. Inspiration for these practices came from knowing what effective software engineers do every day.</p><h2 id="environment-management" tabindex="-1">Environment management <a class="header-anchor" href="#environment-management" aria-label="Permalink to &quot;Environment management&quot;">​</a></h2><p>In the updated <a href="https://docs.claude.com/en/docs/build-with-claude/prompt-engineering/claude-4-best-practices#multi-context-window-workflows" target="_blank" rel="noreferrer">Claude 4 prompting guide</a>, we shared some best practices for multi-context window workflows, including a harness structure that uses “a different prompt for the very first context window.” This “different prompt” requests that the initializer agent set up the environment with all the necessary context that future coding agents will need to work effectively. Here, we provide a deeper dive on some of the key components of such an environment.</p><h3 id="feature-list" tabindex="-1">Feature list <a class="header-anchor" href="#feature-list" aria-label="Permalink to &quot;Feature list&quot;">​</a></h3><p>To address the problem of the agent one-shotting an app or prematurely considering the project complete, we prompted the initializer agent to write a comprehensive file of feature requirements expanding on the user’s initial prompt. In the <a href="http://claude.ai/redirect/website.v1.52cdad0c-ca7a-43d0-8297-e3f64b8a4f36" target="_blank" rel="noreferrer">claude.ai</a> clone example, this meant over 200 features, such as “a user can open a new chat, type in a query, press enter, and see an AI response.” These features were all initially marked as “failing” so that later coding agents would have a clear outline of what full functionality looked like.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{</span></span>
<span class="line"><span>    &quot;category&quot;: &quot;functional&quot;,</span></span>
<span class="line"><span>    &quot;description&quot;: &quot;New chat button creates a fresh conversation&quot;,</span></span>
<span class="line"><span>    &quot;steps&quot;: [</span></span>
<span class="line"><span>      &quot;Navigate to main interface&quot;,</span></span>
<span class="line"><span>      &quot;Click the &#39;New Chat&#39; button&quot;,</span></span>
<span class="line"><span>      &quot;Verify a new conversation is created&quot;,</span></span>
<span class="line"><span>      &quot;Check that chat area shows welcome state&quot;,</span></span>
<span class="line"><span>      &quot;Verify conversation appears in sidebar&quot;</span></span>
<span class="line"><span>    ],</span></span>
<span class="line"><span>    &quot;passes&quot;: false</span></span>
<span class="line"><span>  }</span></span></code></pre></div><p>We prompt coding agents to edit this file only by changing the status of a passes field, and we use strongly-worded instructions like “It is unacceptable to remove or edit tests because this could lead to missing or buggy functionality.” After some experimentation, we landed on using JSON for this, as the model is less likely to inappropriately change or overwrite JSON files compared to Markdown files.</p><h3 id="incremental-progress" tabindex="-1">Incremental progress <a class="header-anchor" href="#incremental-progress" aria-label="Permalink to &quot;Incremental progress&quot;">​</a></h3><p>Given this initial environment scaffolding, the next iteration of the coding agent was then asked to work on only one feature at a time. This incremental approach turned out to be critical to addressing the agent’s tendency to do too much at once.</p><p>Once working incrementally, it’s still essential that the model leaves the environment in a clean state after making a code change. In our experiments, we found that the best way to elicit this behavior was to ask the model to commit its progress to git with descriptive commit messages and to write summaries of its progress in a progress file. This allowed the model to use git to revert bad code changes and recover working states of the code base.</p><p>These approaches also increased efficiency, as they eliminated the need for an agent to have to guess at what had happened and spend its time trying to get the basic app working again.</p><h3 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;Testing&quot;">​</a></h3><p>One final major failure mode that we observed was Claude’s tendency to mark a feature as complete without proper testing. Absent explicit prompting, Claude tended to make code changes, and even do testing with unit tests or <code>curl</code> commands against a development server, but would fail recognize that the feature didn’t work end-to-end.</p><p>In the case of building a web app, Claude mostly did well at verifying features end-to-end once explicitly prompted to use browser automation tools and do all testing as a human user would.</p><p><img src="https://www.anthropic.com/_next/image?url=https%3A%2F%2Fwww-cdn.anthropic.com%2Fimages%2F4zrzovbb%2Fwebsite%2Ff94c2257964fb2d623f1e81f874977ebfc0986bc-1920x1080.gif&amp;w=3840&amp;q=75" alt=" Screenshots taken by Claude through the Puppeteer MCP server as it tested the claude.ai clone. "></p><p>Screenshots taken by Claude through the Puppeteer MCP server as it tested the claude.ai clone.</p><p>Providing Claude with these kinds of testing tools dramatically improved performance, as the agent was able to identify and fix bugs that weren’t obvious from the code alone.</p><p>Some issues remain, like limitations to Claude’s vision and to browser automation tools making it difficult to identify every kind of bug. For example, Claude can’t see browser-native alert modals through the Puppeteer MCP, and features relying on these modals tended to be buggier as a result.</p><h2 id="getting-up-to-speed" tabindex="-1">Getting up to speed <a class="header-anchor" href="#getting-up-to-speed" aria-label="Permalink to &quot;Getting up to speed&quot;">​</a></h2><p>With all of the above in place, every coding agent is prompted to run through a series of steps to get its bearings, some quite basic but still helpful:</p><ol><li><em>Run <code>pwd</code> to see the directory you’re working in. You’ll only be able to edit files in this directory.</em></li><li><em>Read the git logs and progress files to get up to speed on what was recently worked on.</em></li><li><em>Read the features list file and choose the highest-priority feature that’s not yet done to work on.</em></li></ol><p>This approach saves Claude some tokens in every session since it doesn’t have to figure out how to test the code. It also helps to ask the initializer agent to write an init.sh script that can run the development server, and then run through a basic end-to-end test before implementing a new feature.</p><p>In the case of the claude.ai clone, this meant that the agent always started the local development server and used the Puppeteer MCP to start a new chat, send a message, and receive a response. This ensured that Claude could quickly identify if the app had been left in a broken state, and immediately fix any existing bugs. If the agent had instead started implementing a new feature, it would likely make the problem worse.</p><p>Given all this, a typical session starts off with the following assistant messages:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[Assistant] I&#39;ll start by getting my bearings and understanding the current state of the project.</span></span>
<span class="line"><span>[Tool Use] &lt;bash - pwd&gt;</span></span>
<span class="line"><span>[Tool Use] &lt;read - claude-progress.txt&gt;</span></span>
<span class="line"><span>[Tool Use] &lt;read - feature_list.json&gt;</span></span>
<span class="line"><span>[Assistant] Let me check the git log to see recent work.</span></span>
<span class="line"><span>[Tool Use] &lt;bash - git log --oneline -20&gt;</span></span>
<span class="line"><span>[Assistant] Now let me check if there&#39;s an init.sh script to restart the servers.</span></span>
<span class="line"><span>&lt;Starts the development server&gt;</span></span>
<span class="line"><span>[Assistant] Excellent! Now let me navigate to the application and verify that some fundamental features are still working.</span></span>
<span class="line"><span>&lt;Tests basic functionality&gt;</span></span>
<span class="line"><span>[Assistant] Based on my verification testing, I can see that the fundamental functionality is working well. The core chat features, theme switching, conversation loading, and error handling are all functioning correctly. Now let me review the tests.json file more comprehensively to understand what needs to be implemented next.</span></span>
<span class="line"><span>&lt;Starts work on a new feature&gt;</span></span></code></pre></div><p>Agent failure modes and solutions</p><table tabindex="0"><thead><tr><th><strong>Problem</strong></th><th><strong>Initializer Agent Behavior</strong></th><th><strong>Coding Agent Behavior</strong></th></tr></thead><tbody><tr><td>Claude declares victory on the entire project too early.</td><td>Set up a feature list file: based on the input spec, set up a structured JSON file with a list of end-to-end feature descriptions.</td><td>Read the feature list file at the beginning of a session. Choose a single feature to start working on.</td></tr><tr><td>Claude leaves the environment in a state with bugs or undocumented progress.</td><td>An initial git repo and progress notes file is written.</td><td>Start the session by reading the progress notes file and git commit logs, and run a basic test on the development server to catch any undocumented bugs. End the session by writing a git commit and progress update.</td></tr><tr><td>Claude marks features as done prematurely.</td><td>Set up a feature list file.</td><td>Self-verify all features. Only mark features as “passing” after careful testing.</td></tr><tr><td>Claude has to spend time figuring out how to run the app.</td><td>Write an <code>init.sh</code> script that can run the development server.</td><td>Start the session by reading <code>init.sh</code>.</td></tr></tbody></table><p>Summarizing four common failure modes and solutions in long-running AI agents.</p><h2 id="future-work" tabindex="-1">Future work <a class="header-anchor" href="#future-work" aria-label="Permalink to &quot;Future work&quot;">​</a></h2><p>This research demonstrates one possible set of solutions in a long-running agent harness to enable the model to make incremental progress across many context windows. However, there remain open questions.</p><p>Most notably, it’s still unclear whether a single, general-purpose coding agent performs best across contexts, or if better performance can be achieved through a multi-agent architecture. It seems reasonable that specialized agents like a testing agent, a quality assurance agent, or a code cleanup agent, could do an even better job at sub-tasks across the software development lifecycle.</p><p>Additionally, this demo is optimized for full-stack web app development. A future direction is to generalize these findings to other fields. It’s likely that some or all of these lessons can be applied to the types of long-running agentic tasks required in, for example, scientific research or financial modeling.</p><h3 id="acknowledgements" tabindex="-1">Acknowledgements <a class="header-anchor" href="#acknowledgements" aria-label="Permalink to &quot;Acknowledgements&quot;">​</a></h3><p>Written by Justin Young. Special thanks to David Hershey, Prithvi Rajasakeran, Jeremy Hadfield, Naia Bouscal, Michael Tingley, Jesse Mu, Jake Eaton, Marius Buleandara, Maggie Vo, Pedram Navid, Nadine Yasser, and Alex Notov for their contributions.</p><p>This work reflects the collective efforts of several teams across Anthropic who made it possible for Claude to safely do long-horizon autonomous software engineering, especially the code RL &amp; Claude Code teams. Interested candidates who would like to contribute are welcome to apply at <a href="http://anthropic.com/careers" target="_blank" rel="noreferrer">anthropic.com/careers</a>.</p><h3 id="footnotes" tabindex="-1">Footnotes <a class="header-anchor" href="#footnotes" aria-label="Permalink to &quot;Footnotes&quot;">​</a></h3><p>1. We refer to these as separate agents in this context only because they have different initial user prompts. The system prompt, set of tools, and overall agent harness was otherwise identical.</p>`,48)])])}const g=t(o,[["render",i]]);export{u as __pageData,g as default};
