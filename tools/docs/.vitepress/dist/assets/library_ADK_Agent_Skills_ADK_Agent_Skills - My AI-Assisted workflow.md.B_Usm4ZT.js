import{_ as t,o as a,c as i,ag as s}from"./chunks/framework.eQLvDOR_.js";const u=JSON.parse('{"title":"My AI-Assisted workflow","description":"","frontmatter":{"title":"My AI-Assisted workflow","source":"https://www.maiobarbero.dev/articles/ai-assisted-workflow/#step-1-free-form-plan","clipped_at":"2026-04-18","category":"ADK_Agent_Skills","tags":["workflow","prd","ai-assisted"],"status":"distilled","compiled":true},"headers":[],"relativePath":"library/ADK_Agent_Skills/ADK_Agent_Skills - My AI-Assisted workflow.md","filePath":"library/ADK_Agent_Skills/ADK_Agent_Skills - My AI-Assisted workflow.md","lastUpdated":null}'),o={name:"library/ADK_Agent_Skills/ADK_Agent_Skills - My AI-Assisted workflow.md"};function n(r,e,h,l,d,c){return a(),i("div",null,[...e[0]||(e[0]=[s('<p>You open a chat, describe what you want, iterate on the output, and ship something that more or less works. It feels fast. The features work, technically. But nobody, <strong>including me</strong>, fully understood what is there. Edge cases nobody thought to handle, architecture that made sense in the moment but didn’t survive contact with the next feature. A growing sense that I was building faster and understanding less.</p><p>The problem I kept running into was this: how do you get the speed benefits of AI assistance without losing the clarity and intentionality that makes software maintainable? The short answer is that the real work happens before the coding starts.</p><h2 id="the-core-idea-thinking-in-writing-not-in-code" tabindex="-1">The core idea: thinking in writing, not in code <a class="header-anchor" href="#the-core-idea-thinking-in-writing-not-in-code" aria-label="Permalink to &quot;The core idea: thinking in writing, not in code&quot;">​</a></h2><p>What is AI actually good at? Implementation. What is it genuinely bad at? Figuring out what you actually want, catching the assumptions you forgot to make explicit, and telling you when your mental model of the problem is wrong. <strong>That’s your job. It will always be your job.</strong></p><p>The single most valuable shift I made was treating every feature as a thinking problem first and an implementation problem second. The workflow is designed to force that thinking to happen before any code is written, and to use AI to stress-test it, not to skip it.</p><p>This workflow has been adapted to work with me from Mark Pocock’s <a href="https://github.com/mattpocock/skills/tree/main" target="_blank" rel="noreferrer">skills</a>.</p><h2 id="the-workflow" tabindex="-1">The workflow <a class="header-anchor" href="#the-workflow" aria-label="Permalink to &quot;The workflow&quot;">​</a></h2><h3 id="step-1-free-form-plan" tabindex="-1">Step 1: Free-form plan <a class="header-anchor" href="#step-1-free-form-plan" aria-label="Permalink to &quot;Step 1: Free-form plan&quot;">​</a></h3><p>Everything starts with a document I write myself, in plain language, with no required structure. I describe the problem, my initial thinking about the solution, the constraints I’m aware of, and the things I’m uncertain about. This is not a deliverable, nobody reads it but me. Its only purpose is to get the thinking out of my head and into a form I can examine.</p><p>The quality of everything downstream depends entirely on the quality of this step. A vague plan produces a vague <strong><a href="https://www.maiobarbero.dev/glossary/#prd" target="_blank" rel="noreferrer">PRD</a></strong>, which produces vague issues, which produces code that technically runs but doesn’t do what you meant.</p><h3 id="step-2-prd-via-write-a-prd" tabindex="-1">Step 2: PRD via write-a-prd <a class="header-anchor" href="#step-2-prd-via-write-a-prd" aria-label="Permalink to &quot;Step 2: PRD via write-a-prd&quot;">​</a></h3><p>The free-form plan becomes the input to a structured interview process. The skill explores the codebase to understand the current state of things, then interviews me relentlessly about every aspect of the plan, walking down each branch of the design tree, resolving dependencies between decisions one by one. This is the step where bad ideas get caught. Not because the AI is smarter than me, but because being forced to answer specific questions about your own plan reveals the places where you were hand-waving. “How does this behave when the user isn’t authenticated?” “What happens if this operation partially fails?” “You said this replaces the existing feature, what happens to users who depend on the current behavior?”</p><p>The output is a structured <strong>PRD</strong> file containing a problem statement, a solution description, an extensive list of user stories, implementation decisions (modules, interfaces, schema changes, API contracts), module design, testing decisions, and explicit out-of-scope items. Everything is <strong>explicit</strong>. The user stories are the backbone of everything that follows. They need to be specific enough that acceptance criteria can be derived from them unambiguously downstream, not at this stage, but when scope is defined at the issue level.</p><h3 id="step-3-issues-via-prd-to-issues" tabindex="-1">Step 3: Issues via prd-to-issues <a class="header-anchor" href="#step-3-issues-via-prd-to-issues" aria-label="Permalink to &quot;Step 3: Issues via prd-to-issues&quot;">​</a></h3><p>The <strong>PRD</strong> becomes a set of issues using <strong><a href="https://www.maiobarbero.dev/glossary/#vertical-slice" target="_blank" rel="noreferrer">vertical slices</a></strong>, tracer bullets that cut through every integration layer end-to-end rather than horizontal slices of a single layer. A slice that only touches the database, or only touches the UI, is not a valid slice. Each issue should deliver a narrow but complete path that is demoable or verifiable on its own. Each issue is classified as either <strong><a href="https://www.maiobarbero.dev/glossary/#afk" target="_blank" rel="noreferrer">AFK</a></strong> (the AI can implement and the change can be merged without human interaction) or <strong><a href="https://www.maiobarbero.dev/glossary#hitl" target="_blank" rel="noreferrer">HITL</a></strong> (a human decision is required at some point during implementation). Preferring AFK over HITL wherever possible keeps the work moving without becoming a bottleneck on my attention.</p><p>Before anything is written, the skill presents the proposed breakdown and asks: does the granularity feel right, are the dependency relationships correct, should anything be merged or split? Issues are written in dependency order so that cross-references between them use real numbers.</p><p>Each issue contains a concise description of the end-to-end behavior, a “how to verify” section describing exactly how to confirm the slice is complete, acceptance criteria in Given/When/Then format including error cases, a list of blockers, and references back to the user stories it addresses. Everything lives in files. I work across different platforms, sometimes GitHub, sometimes GitLab, and keeping the workflow file-based means it doesn’t depend on any particular tool.</p><h3 id="step-4-tasks-via-issues-to-tasks" tabindex="-1">Step 4: Tasks via issues-to-tasks <a class="header-anchor" href="#step-4-tasks-via-issues-to-tasks" aria-label="Permalink to &quot;Step 4: Tasks via issues-to-tasks&quot;">​</a></h3><p>Each issue is broken down into concrete, ordered tasks, one task per focused AI session. The constraint is deliberate: if a task can’t be completed in a single session, it’s too large. The skill explores the specific parts of the codebase touched by the issue, identifies existing patterns to follow, and produces a task list with types (WRITE, TEST, MIGRATE, CONFIG, REVIEW), explicit outputs, and dependency order. Schema before logic, logic before API, API before UI, tests interleaved rather than batched at the end.</p><p>The key design decision in the task descriptions: they are written as instructions to the AI that will execute them, not as notes to a human developer. Each task specifies which files to touch, which existing patterns to follow, and what the output looks like when done. No code snippets, just intent, not implementation.</p><h3 id="step-5-the-handoff-to-code" tabindex="-1">Step 5: The handoff to code <a class="header-anchor" href="#step-5-the-handoff-to-code" aria-label="Permalink to &quot;Step 5: The handoff to code&quot;">​</a></h3><p>Each task description is a self-contained prompt. When I’m ready to implement a task, I open a fresh session and paste the task description along with the parent issue for context. The task description was written for this purpose, it specifies scope, references the right files and patterns, and defines what done looks like. Fresh context per task is intentional. Long sessions with accumulated context tend to drift: the model starts making decisions based on what it already did rather than what the task requires. Starting clean with a well-scoped task consistently produces better output than continuing a long session. For REVIEW tasks, the ones flagged as requiring a human decision, I stop, make the decision, update the task file with the outcome, and continue. These are the moments where the workflow earns its keep: the decision is made deliberately, in context, not buried in a long generation.</p><h3 id="step-6-code-review-via-code-review" tabindex="-1">Step 6: Code review via code-review <a class="header-anchor" href="#step-6-code-review-via-code-review" aria-label="Permalink to &quot;Step 6: Code review via code-review&quot;">​</a></h3><p>Every PR goes through a structured six-pass review before merge. The passes cover logic errors, operation ordering, bad practices, security, magic strings and values, and pattern improvements.</p><p>Operation ordering deserves particular attention in AI-generated code. Models tend to produce code that does the right things but sometimes in the wrong sequence: sending a notification before committing a transaction, writing an audit log after the action it should record, mutating state before validating input. These bugs are easy to miss in review because the code looks correct at a glance.</p><p>The review runs on a file or diff, not on the whole feature. Scope is deliberately narrow, catching issues at the PR level is far cheaper than finding them later.</p><h3 id="step-7-final-audit-via-final-audit" tabindex="-1">Step 7: Final audit via final-audit <a class="header-anchor" href="#step-7-final-audit-via-final-audit" aria-label="Permalink to &quot;Step 7: Final audit via final-audit&quot;">​</a></h3><p>At the end of a feature, a cross-cutting audit looks at things that can only be evaluated across the whole implementation. Not individual bugs, those should have been caught per PR, but systemic issues: inconsistencies between modules, patterns that were introduced early and replicated incorrectly everywhere, security assumptions that hold in isolation but break down across the full surface area.</p><p>The audit reads the full implementation before flagging anything, which is the point. It groups findings by severity, gives an explicit overall verdict on whether the feature is safe to leave in production, and asks for approval before making any changes. Unsupervised fixes on already-merged code are riskier than fixes made during development.</p><h2 id="what-this-workflow-is-not" tabindex="-1">What this workflow is not <a class="header-anchor" href="#what-this-workflow-is-not" aria-label="Permalink to &quot;What this workflow is not&quot;">​</a></h2><p>It is not fast to set up. The planning and <strong>PRD</strong> steps take real time, and the temptation to skip them in favor of going straight to code is constant. The workflow only pays off if you genuinely believe that thinking time before coding is cheaper than debugging time after.</p><p>It is also not a replacement for engineering judgment. The AI will suggest reasonable things at every step that are wrong for your specific situation. The review steps, where you evaluate the breakdown before anything is created, exist precisely because the AI’s output needs to be validated against knowledge it doesn’t have: your team’s conventions, your users’ actual behavior, the parts of the codebase that have hidden complexity.</p><h2 id="the-underlying-principle" tabindex="-1">The underlying principle <a class="header-anchor" href="#the-underlying-principle" aria-label="Permalink to &quot;The underlying principle&quot;">​</a></h2><p>Every step in this workflow has the same structure: AI produces something, you review it with full context, then it gets created. The AI accelerates the production. <strong>The review is yours, always</strong>.</p><p>The workflow is designed to make that review as effective as possible, by ensuring that when you’re evaluating an issue, you have a <strong>PRD</strong> to check it against; when you’re evaluating a task, you have an issue to check it against; and when you’re reviewing code, you have acceptance criteria to check it against.</p><h2 id="the-skills" tabindex="-1">The skills <a class="header-anchor" href="#the-skills" aria-label="Permalink to &quot;The skills&quot;">​</a></h2><p>Now if you read this article to the end you at least deserve a link with my skills. Check my <a href="https://github.com/maiobarbero/my-ai-workflow" target="_blank" rel="noreferrer">GitHub repo</a>.</p>',37)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
